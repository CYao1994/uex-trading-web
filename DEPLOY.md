# UEX Trade Navigator - ????

## EdgeOne Pages ????(???? ?)

### ????

```
EdgeOne Pages
??? ????(?? React SPA)
?   ??? index.html
?   ??? assets/(JS/CSS/??)
?   ??? favicon.svg, icons.svg
??? Cloud Functions(?? FastAPI)
?   ??? cloud-functions/api/
?       ??? index.py          # FastAPI ??
?       ??? requirements.txt  # Python ??
?       ??? api/, services/   # ???????
??? edgeone.json              # ??????
```

---

## ???:GitHub ????(?? ?)

Push ? GitHub ??????????,???????

### ????

```
GitHub Push ? EdgeOne ???? ? npm run build(???) ? ?? dist/
```

`npm run build` ?? `scripts/build.js`,??:
1. ??????(`cd frontend && npm install`)
2. ????(??? `dist/`)
3. ?? `cloud-functions/` ? `edgeone.json` ? `dist/`
4. `dist/` ????????

### EdgeOne ?????

? EdgeOne Pages ??? ? ???? ? ???????:

| ??? | ? | ?? |
|--------|-----|------|
| **????** | `npm run build` | ??? package.json ? build ?? |
| **????** | `dist` | ?????? |
| **Node.js ??** | 18 ? 20 | Vite 8 ?? Node ? 18 |
| **???** | `/`(??? `/`) | ????? |

### ????

? EdgeOne Pages ??? ? ???? ? ???????:

| ??? | ? | ?? |
|--------|-----|------|
| `UEX_API_KEY` | ?? UEX API Key | ???? API ??????? |

### ???????(??)

????????,????? eo_token ??,?????

---

## ???:CLI ????

???????????,??????????

### ??

```bash
# 1. ??(??? EdgeOne ???????)
npm run build

# 2. ??
npx edgeone pages deploy -n uex-trading-web ./dist
```

?? **??**:??????? GitHub Provider,CLI ?????????? Upload Provider ????

---

## ????(???)

| ?? | ???? |
|------|---------|
| ???? | ?? |
| Cloud Functions ?? | 100 ??/? |
| Cloud Functions ???? | 50 ? GBs/? |
| KV ?? | 1GB |
| ???? | 500 ?/? |

### ??????

1. **??????**:EdgeOne Cloud Functions ?? `/api/*` ???????? `/api` ??,?? FastAPI router ??? prefix
2. **????**:?? `baseURL: '/api'`,?? CORS
3. **Cloud Functions ??**:`cloud-functions/api/index.py` ?????? `app` ??
4. **Python ???**:Python 3.10.11,????? 128MB,?? 1GB,?? 120s

---

## ????

### ????

- Node.js 18+
- Python 3.10+
- UEX API Key(??)

### ????

```bash
# ?? 1:????
cd cloud-functions/api
pip install -r requirements.txt
UEX_API_KEY=your_key python -m uvicorn index:app --host 0.0.0.0 --port 8000 --reload

# ?? 2:????
cd frontend
npm install --legacy-peer-deps
npm run dev
# ?? http://localhost:5173
# API ?? http://localhost:8000/docs
```

Vite ????????? `/api/*` ????? `localhost:8000`,??? `/api` ???

---

## ????????

| ?? | ?? |
|------|------|
| `package.json` | ???????(`npm run build`) |
| `scripts/build.js` | ????(????????????????) |
| `edgeone.json` | EdgeOne Pages ?????? |
| `cloud-functions/api/index.py` | Cloud Functions ??(FastAPI app) |
| `cloud-functions/api/requirements.txt` | Python ?? |
| `frontend/vite.config.js` | Vite ???? + ???? |
| `frontend/src/api/client.js` | API ???(?? /api) |
| `.gitignore` | Git ???? |
